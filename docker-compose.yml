version: "3.9"

services:
  meeting-ai:
    build: .
    image: meeting-ai:latest
    container_name: meeting-ai
    ports:
      - "8501:8501"
    volumes:
      - ./outputs:/app/outputs
    env_file:
      - .env
    restart: unless-stopped
